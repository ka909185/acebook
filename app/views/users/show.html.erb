<div class="child row">
  <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <div class="blog-text">
      <p class="left"><H2><strong><%= @user.name %>さんの友達つながり</strong></H2></p>

    </div>
    <div class="followed_users">
      <p><H3><strong>フォローしている友達</strong></H3></p>
      <% @followed_users.each do |f|%>
        <%= f.name %>
        <%= profile_img(f) %>
      <% end %>
    </div>
    <div class="followers">
      <p><H3><strong><%= @user.name %>さんをフォローしている友達</strong></H3></p>
      <% @followers.each do |f|%>
        <%= f.name %>
        <%= profile_img(f) %>
      <% end %>
    </divs>
  </div>
  <br>
  <%= link_to "Back", :back %>
</div>
